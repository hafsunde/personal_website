# Codex Instructions

Before doing any substantive work in this repository, read `CLAUDE.md` and follow
its project context, writing rules, source rules, and workflow conventions.

Treat `CLAUDE.md` as the canonical project instruction file. This `AGENTS.md`
exists only to make Codex load that context at the start of a session.

